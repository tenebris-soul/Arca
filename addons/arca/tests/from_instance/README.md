# from_instance Test

Open and run this scene in Godot:

```text
res://addons/arca/tests/from_instance/from_instance_test_runner.tscn
```

The scene checks exact instance binding, singleton behavior, child-script binding to a base script, rejection of unrelated instances, and container tracking.
